# Optional: used for canonical URLs, sitemap, and Open Graph. Defaults to https://janitorialappointments.com if not set.
# NEXT_PUBLIC_SITE_URL=https://janitorialappointments.com

# Optional: Google Tag Manager container ID (e.g. GTM-XXXXXXX). If set, GTM script and noscript fallback are injected on the site layout.
# 

# Optional: Google Search Console verification meta tag content. Do NOT hardcode the full meta tag; set only the value of the content attribute (e.g. the long string from Search Console).
# 

# Contact form: email address to receive new contact submissions (default: shariar.ayon128@gmail.com).
# CONTACT_NOTIFY_EMAIL=shariar.ayon128@gmail.com

# Resend (required for contact notification emails and other transactional emails).
# RESEND_API_KEY=re_xxxx
# RESEND_FROM_EMAIL=onboarding@resend.dev
